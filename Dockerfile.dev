# 開発環境用Dockerfile
FROM node:20-alpine

# 開発に必要なツールをインストール
RUN apk add --no-cache git

WORKDIR /app

# 依存関係のインストール
COPY package*.json ./
RUN npm install

# ソースコードをコピー（ボリュームマウントで上書きされるが初期状態として必要）
COPY . .

# 開発サーバーのポートを公開
EXPOSE 5173 3000

# ヘルスチェック
HEALTHCHECK --interval=30s --timeout=3s --start-period=10s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:5173/ || exit 1

# 開発サーバーを起動（ホットリロード有効）
CMD ["npm", "run", "dev"]
