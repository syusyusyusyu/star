{
  "name": "star",
  "private": true,
  "version": "0.1.0",
  "description": "Hono-based server for static pages and simple APIs",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "NODE_ENV=development node server.mjs",
    "start": "NODE_ENV=production node server.mjs",
    "build": "node build.mjs",
    "preview": "NODE_ENV=production PORT=4173 node server.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^1.11.0",
    "hono": "^4.5.0"
  }
}
